const config = {
  // basename: 仅在构建时设置，不要在BASENAME末尾添加'/'，也不要只使用'/'，而是使用空白('')
  // 比如 '/berry-material-react/react/default'
  basename: '/free',          // 设置应用的基本URL路径
  defaultPath: '/dashboard/default',  // 设置默认路径
  fontFamily: `'Roboto', sans-serif`,  // 设置应用的字体
  borderRadius: 12          // 设置应用中使用的边框半径大小
};

export default config;
